[project]
name = "mon"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pyxel>=2.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
